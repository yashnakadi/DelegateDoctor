"""Repair rules.

The prototype ships exactly one, DD-001. There is deliberately no registry or
plugin mechanism: with a single rule, a dynamic lookup would add indirection
without adding capability. Add the second rule first, then decide what (if
anything) the two have in common.
"""
